import type { IFrontendInfo } from '../IFrontend.js';

/**
 * Stub frontend info for API (no UI needed)
 */
export const Info: IFrontendInfo = {
    ID: 'api',
    Identifier: 'api',
    Label: 'API',
};
